import axiosApi from '../../services/axiosApi';
import { setCookie, removeCookie, getCookie } from '../../services/cookies';

export const signin = (email, password, otp = false, hash = '') => {
  return async dispatch => {
    try {
    } catch (error) {}
  };
};

export const signOut = () => {
  return async dispatch => {
    try {
    } catch (error) {
      console.log(error);
    }
  };
};
